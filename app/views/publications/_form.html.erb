<%= form_with(model: publication) do |form| %>
  <% if publication.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(publication.errors.count, "error") %> evitó que esta publicación se guardara:</h2>

      <ul>
        <% publication.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-2">
    <%= form.label :nombre, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div class="mb-2">
    <%= form.label :descripción, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div class="form-group">
    <%= form.label :imágenes %>
    <%= form.file_field :images, multiple: true, class: 'form-control' %>
  </div>

  <div>
    <%= form.submit "Enviar", class: "btn btn-primary text-white mt-3" %>
  </div>
<% end %>
