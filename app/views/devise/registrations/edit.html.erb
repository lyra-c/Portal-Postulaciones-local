<h2 class="mt-3 mb-4">Editar perfil <!-- < %= resource_name.to_s.humanize %> --> </h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
  </div>

  <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
    <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
  <% end %>

  <div class="field">
    <%= f.label :contraseña %> <i>(deja en blanco si no quieres cambiarla)</i><br />
    <%= f.password_field :password, autocomplete: "new-password" %>
    <% if @minimum_password_length %>
      <br />
      <em><%= @minimum_password_length %> caracteres como mínimo</em>
    <% end %>
  </div>

  <div class="field">
    <%= f.label :confirmar_contraseña %><br />
    <%= f.password_field :password_confirmation, autocomplete: "new-password" %>
  </div>

  <div class="field">
    <%= f.label :contraseña_actual %> <i>(necesitamos tu contraseña actual para confirmar los cambios)</i><br />
    <%= f.password_field :current_password, autocomplete: "current-password" %>
  </div>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :name, autocomplete: "Nombre de usuario" %>
  </div>

  <div class="field">
    <%= f.label :edad %><br />
    <%= f.text_field :age, autocomplete: "Ingresa tu edad" %>
  </div>

  <div class="field">
    <%= f.label :descripción_laboral %><br />
    <%= f.text_field :description, autocomplete: "Añade una descripción" %>
  </div>

  <div>
    <p class="fw-bold">Foto de perfil (opcional): <p> <br>
    <%= f.file_field :images, multiple: true, class: 'form-control' %>
  </div>

  <div class="actions">
    <%= f.submit "Enviar", class: "btn btn-primary text-white my-2" %>
  </div>
<% end %>
<!-- Comenté esta sección para que no se pudiera eliminar la cuenta del jefe -->
<!-- 
<h3>Cancel my account</h3>

<div>Unhappy? < %= button_to "Cancel my account", registration_path(resource_name), data: { confirm: "Are you sure?", turbo_confirm: "Are you sure?" }, method: :delete %></div> -->

<%= link_to "Volver atrás", :back, class: "btn btn-info text-white my-3" %>
